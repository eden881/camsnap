version: 2

run:
  timeout: 3m
  tests: true

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - revive
  disable:
    - depguard

issues:
  exclude-use-default: false
  exclude-dirs:
    - vendor

severity:
  default-severity: error
